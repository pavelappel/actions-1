name: my first workflow
on: push 
jobs:
    first_job:
        runs_on: ubuntu-latest
        steps:
            - name: welcome messgae
              run: echo "my first github acions job"

            - name: list file
              run: ls
            
            - name: read file
              run: cat README.md